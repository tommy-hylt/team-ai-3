# codex-gpt-4.1-mini

Use this agent when:
- you want an OpenAI-based coding agent for implementation-heavy work
- you want to diversify quota across providers

Common models (Codex CLI supports `-m/--model`):
- `gpt-4.1-mini` (fast/cheaper)
- `gpt-4.1` (stronger)

Command notes:
- This template runs Codex in non-interactive quiet mode (`-q`) with `-a suggest`.
- Default model: `gpt-4.1-mini`.
